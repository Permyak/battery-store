<% if signed_in? && admin? %>
<h2 align=center>Редактирование моделей</h2>
<div class="container">
	<div class="row">
		<div class="span6 offset1">
			<%= render 'form' %>
			
			<%= link_to 'Показать', @model%> |
			<%= link_to 'Назад', models_path %>
		</div>
	</div>
</div>
<% end %>
