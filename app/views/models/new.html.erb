<% if signed_in? && admin? %>
<h2 align=center>Новая модель</h2>
<div class="container">
	<div class="row">
	<div class="span6 offset1">
		<%= render 'form' %>
		</div>
	</div>
</div>

<%= link_to 'Назад', batteries_path %>
<% end %>