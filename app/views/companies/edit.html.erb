<% if signed_in? && admin? %>
<h1>Editing company</h1>

<div class="container">
	<div class="row">
	<div class="span6 offset1">
		<%= render 'form' %>
		
			<%= link_to 'Show', @company %> |
			<%= link_to 'Back', companies_path %>
		</div>
	</div>
</div>

<% end %>