<div class="center hero-unit">
  <h2>
    Game's stats and ratings
  </h2>
  <%= form_tag('search', method: "get") do %>
	<%= text_field_tag "steamid", nil, :placeholder => "Steam user's name, e.g. qshaman" %>
    <%= submit_tag("Go") %>
  <% end %>
</div>
